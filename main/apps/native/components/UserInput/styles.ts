import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    fontSize: 30,
    fontWeight: "bold",
    textAlign: "center",
    borderColor: '#333333',
    borderRadius: 5,
    borderWidth: 1,
    marginBottom: 20,
  },
});

export default styles;
